
import express from 'express';

const router = express.Router();

// Authentication is handled directly via PocketBase SDK on the frontend.
// This file is kept clean to prevent any interference with the frontend auth flow.

export default router;
