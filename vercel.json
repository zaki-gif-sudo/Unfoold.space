{
  "version": 2,
  "buildCommand": "npm run build --prefix apps/web",
  "outputDirectory": "apps/web/dist",
  "framework": "vite",
  "devCommand": "npm run dev --prefix apps/web",
  "rewrites": [
    {
      "source": "/pb/:path*",
      "destination": "/api/pb"
    },
    {
      "source": "/((?!assets|logos|manifest.json|service-worker.js|api).*)",
      "destination": "/index.html"
    }
  ]
}
